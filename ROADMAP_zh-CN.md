# CameraRig 开发路线图

## R0 仓库初始化

建立可安装、可测试的 Python 仓库、文档基线、命令行入口和持续集成检查，不提供相机硬件功能。

## R1 核心契约与严格配置

冻结单相机数据模型、SE(3) 语义、严格配置、错误模型、目标插件接口和 JSON 产物基础，且不连接硬件。

## R2 D435i 设备发现与生命周期

增加明确的 RealSense 设备选择、型号与序列号验证以及单设备生命周期管理。

状态：已实现，并已在物理 D435i 上验证。

## R3 RealSense 原厂标定导出

读取并导出单设备内部的原厂内参、深度比例和数据流间变换。

状态：已实现，并已使用物理 D435i 的实际启用 profiles 验证。

## R4 单相机原始采集与设备内数据流验证

采集一个物理相机的 RGB、深度与红外原始数据流，并验证设备内部时间戳与帧关系。

状态：已实现，并已完成物理设备 60 帧重复采集与 300 帧主采集验证。

## R5 快照、产物与重放

持久化已验证的单相机快照，并在无相机硬件时重放。

状态：已实现；物理快照、checksum 校验和无 SDK 重放均已验证。

## R6 TargetDetector 与 ChArUco

使用 ChArUco 实现首个目标检测插件。AprilGrid 仍为计划项，本项不实现。

状态：已实现，并使用经物理尺寸测量、刚性固定的标定板和 D435i 60 帧采集完成硬件验证。

## R7 固定相机外参标定

估计、验证并持久化固定相机到其父坐标系的变换。

状态：已实现，并在物理 D435i 上完成稳健多帧位姿、分半稳定性、原生深度合理性及
坐标轴/重投影诊断验证。

## R8 固定相机一键配置

把设备验证、采集、标定、质量门禁和 bundle 生成组合为可复现的固定相机工作流。

状态：已在物理 D435i 上完成两次验证，包括原子产物发布、完整 checksum、CameraBundle
校验和端到端重复性。

## R9 PCB 集成契约与 v1.0

稳定面向消费者的 CameraBundle 契约以及 1.0 版本的集成边界。

## v1 之后的候选项

- AprilGrid 目标检测插件（已规划，尚未实现）。
- D405 与 D455 驱动。
- Eye-in-hand、robot-world/hand-eye 和 fixed eye-to-hand 标定；腕部相机标定仅预留，尚未实现。
- 其他相机 SDK 驱动。
